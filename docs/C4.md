# SwarmBuild — C4 Diagrams

> Derived from [TECHSPEC.md](./TECHSPEC.md) and the domain language in [CONTEXT.md](../CONTEXT.md).
> Rendered with Mermaid's C4 support. Levels: **Context** (System in its environment) and **Container** (runtime units inside the system).

---

## Level 1 — System Context

How SwarmBuild sits between the people watching the demo and the high-latency Earth link. The whole point: tactical self-heal happens **at the worksite**, never waiting on Earth.

```mermaid
flowchart LR
    evaluator["👤 <b>Evaluator / Operator</b><br/><i>Person</i><br/>Watches the demo, authors the<br/>blueprint, clicks kill / latency /<br/>failure controls"]

    swarmbuild["🛰️ <b>SwarmBuild</b><br/><i>Software System</i><br/>A worksite that re-auctions failed<br/>tasks and heals itself with no<br/>operator in the loop"]

    earth["🌍 <b>Earth (Mission Control)</b><br/><i>External System</i><br/>Remote high-latency side. Sets goals,<br/>receives telemetry. Deliberately<br/>OUTSIDE the tactical loop"]

    evaluator -->|"Views 3D worksite; kill /<br/>latency / failure commands<br/><i>WebSocket</i>"| swarmbuild
    swarmbuild -->|"Telemetry up; goals down<br/><i>earth.uplink (simulated slow link)</i>"| earth
    earth -.->|"High-level goals only<br/>(never tactical)<br/><i>earth.uplink</i>"| swarmbuild

    classDef person fill:#08427b,stroke:#052e56,color:#ffffff
    classDef system fill:#1168bd,stroke:#0b4884,color:#ffffff
    classDef ext fill:#6b6b6b,stroke:#4d4d4d,color:#ffffff
    class evaluator person
    class swarmbuild system
    class earth ext
    linkStyle default stroke:#666,stroke-width:1.5px
```

---

## Level 2 — Container

The runtime units. The **Coordinator process** (Go) holds the four pure deep modules plus the single-writer tick, choreography and in-proc rovers. The **WS Gateway** is the only thing the browser talks to. **NATS** is on the critical path ([ADR-0002](./adr/0002-nats-on-the-critical-path.md)); the optional **rover container** is the encore ([ADR-0001](./adr/0001-in-process-rovers-with-container-encore.md)).

```mermaid
flowchart TB
    evaluator["👤 <b>Evaluator / Operator</b><br/><i>Person</i><br/>Watches and drives the demo"]

    subgraph swarmbuild["🛰️ SwarmBuild"]
        direction TB
        web["<b>Web Dashboard</b><br/><i>React · Vite · react-three-fiber</i><br/>3D lunar scene (2D fallback).<br/>Pure re-render from snapshots.<br/>Never speaks NATS directly"]
        gateway["<b>WS Gateway</b><br/><i>Go</i><br/>Tails NATS, fans out one merged<br/>world snapshot (~10 Hz) +<br/>choreography stream; relays controls"]

        subgraph coordinator["⚙️ Coordinator process · Go"]
            direction LR
            tick["<b>Single-writer tick +<br/>Choreography</b><br/><i>Go goroutine</i><br/>Serialises award vs. expiry<br/>(kills double-assignment)"]
            deep["<b>Deep Modules</b><br/><i>Go — pure, property-tested</i><br/>Allocation · Lease ·<br/>World Model · Task Planner"]
            agents["<b>Robot Agents ×5–6</b><br/><i>Go goroutines, --mode=inproc</i><br/>Behaviour tree, battery drain,<br/>failure injection"]
            adapter["<b>Adapter (the seam)</b><br/><i>Go interface</i><br/>Boundary: core ↔ world"]

            tick -->|"pure decisions<br/><i>in-process</i>"| deep
            agents -->|"acts on / reads world<br/><i>in-process</i>"| adapter
        end

        nats[("<b>NATS server</b><br/><i>NATS · JetStream · KV</i><br/>Bus on the critical path.<br/>KV mirror of World Model")]
        rover["<b>Rover container (encore)</b><br/><i>Go — same binary, --mode=container</i><br/>Optional stretch: a rover as a<br/>separate running system,<br/>killed via docker by a sidecar"]
    end

    earth["🌍 <b>Earth (Mission Control)</b><br/><i>External System</i><br/>High-latency remote side,<br/>outside the tactical loop"]

    evaluator -->|"Views worksite;<br/>clicks controls<br/><i>Browser</i>"| web
    web -->|"Snapshots + events down;<br/>control cmds up<br/><i>WebSocket</i>"| gateway
    gateway -->|"Tails subjects;<br/>relays controls<br/><i>NATS</i>"| nats
    tick -->|"Announces tasks, awards,<br/>KV mirror · <i>NATS</i>"| nats
    agents -->|"Bids, heartbeats,<br/>telemetry · <i>NATS client</i>"| nats
    rover -.->|"Same auction /<br/>heartbeat protocol<br/><i>NATS client</i>"| nats
    nats -->|"Telemetry up; goals down<br/>(latency shim on earth.uplink ONLY)<br/><i>earth.uplink</i>"| earth

    classDef person fill:#08427b,stroke:#052e56,color:#ffffff
    classDef container fill:#1168bd,stroke:#0b4884,color:#ffffff
    classDef db fill:#1168bd,stroke:#0b4884,color:#ffffff
    classDef ext fill:#6b6b6b,stroke:#4d4d4d,color:#ffffff
    classDef encore fill:#3b78c3,stroke:#0b4884,color:#ffffff,stroke-dasharray:4 3
    class evaluator person
    class web,gateway,tick,deep,agents,adapter container
    class nats db
    class rover encore
    class earth ext
    style swarmbuild fill:#f2f6fb,stroke:#a8c2e0,color:#1a1a1a
    style coordinator fill:#e3edf8,stroke:#7aa6d6,color:#1a1a1a
    linkStyle default stroke:#666,stroke-width:1.5px
```

---

## Key relationships (subjects on the bus)

| Subject | Direction | Purpose |
|---|---|---|
| `task.announce` | Coordinator → bus | Task announced for auction |
| `task.bid.<task_id>` | Rover → bus | Rover submits its cost |
| `task.award` | Coordinator → bus | Winner granted a lease |
| `robot.heartbeat.<id>` | Rover → bus | Lease renewal (never latency-shimmed) |
| `robot.telemetry.<id>` | Rover → bus | Position, battery, health, progress |
| `earth.uplink` | ↔ Earth | The **only** place the latency shim lives |

## Notes for readers

- **Deep modules are the product.** Everything else (sim, bus, gateway, web) exists to make those four pure modules *visible*.
- **The bus is on the critical path** by design ([ADR-0002](./adr/0002-nats-on-the-critical-path.md)) — both in-proc and container rovers are NATS clients, so the encore changes hosting, not the protocol.
- **The browser is a pure client** — full snapshots make it reconnect-safe with no client-side simulation drift.
- **Self-heal = expiry + re-auction**, composed with no supervisory logic and no Earth in the loop.
